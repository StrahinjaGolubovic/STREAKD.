# CRITICAL: These environment variables MUST be set in production
# The application will not start without them

# JWT Secret - Used for signing authentication tokens
# SECURITY: Generate a strong random secret (32+ characters)
# Example: openssl rand -hex 32
JWT_SECRET=

# ALTCHA HMAC Key - Used for CAPTCHA verification
# SECURITY: Generate a strong random key (32+ characters)
# Example: openssl rand -hex 32
ALTCHA_HMAC_KEY=

# Database Path (optional, defaults to ./data/gymble.db)
DATABASE_PATH=./data/gymble.db

# Node Environment
NODE_ENV=production

# Admin Usernames (comma-separated list)
# SECURITY: Do not commit actual admin usernames to git
ADMIN_USERNAMES=admin,seuq,jakow,nikola

# SECURITY NOTES:
# 1. NEVER commit .env or .env.local files to git
# 2. Rotate secrets every 90 days
# 3. Use different secrets for development and production
# 4. Store production secrets in a secure secret manager (AWS Secrets Manager, etc.)
